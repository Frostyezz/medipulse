export enum ROLES {
  MEDIC = "MEDIC",
  NURSE = "NURSE",
  PATIENT = "PATIENT",
}

export enum LANGUAGES {
  en = "en",
  ro = "ro",
}

export enum INVITATION_STATUS {
  SENT = "SENT",
  ACCEPTED = "ACCEPTED",
}

export enum REQUEST_STATUS {
  ACCEPTED = "ACCEPTED",
  REJECTED = "REJECTED",
}
